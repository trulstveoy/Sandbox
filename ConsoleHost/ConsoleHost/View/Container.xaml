<UserControl x:Class="ConsoleHost.View.Container"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:view="clr-namespace:ConsoleHost.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <TextBlock Text="{Binding Name}"></TextBlock>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Content="Start" Command="{Binding Start}"></Button>
            <Button Content="Stop" Command="{Binding Stop}"></Button>
            </StackPanel>
        </DockPanel>
        <view:HwndControl Path="..\..\..\TestApp\bin\debug\TestApp.exe" Grid.Row="1" />
    </Grid>
</UserControl>
